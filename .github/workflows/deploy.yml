# XML을 대체하는 문서 / YML, YXML
# 주석을 달수 있다.
# JSON파일을 대체한다는 말이 있다.
name: Deploy React to EC2

on:
  # ~했을 때
  push:
    #push 했을 때
    branches:
      # 브랜치에
      - main
        # 메인 브랜치에

jobs:
  # 작업들
  test:
    # 작업명
    runs-on:
      ubuntu-latest
      # 작업 환경
    steps:
      # 작업들을 순서에 맞춰서 작성
      - name: "testing"
        # 작업명은 testing 이고
        run:
          echo "now test"
          # 작업 내용은 echo "now test"
